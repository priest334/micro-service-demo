eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
spring:
  application:
    name: simple-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service
